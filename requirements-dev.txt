# Development tooling. Install on top of runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

ruff>=0.6
mypy>=1.10
pytest>=8.0
import-linter>=2.1
pre-commit>=3.7
