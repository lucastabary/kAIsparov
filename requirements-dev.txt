# Development tooling. Install on top of runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# ruff and mypy are pinned: a new release can fail CI on untouched code. Keep ruff
# in step with the `rev:` of ruff-pre-commit in .pre-commit-config.yaml.
ruff==0.16.5
mypy==2.3.1
pytest>=8.0
pytest-xdist>=3.5
import-linter>=2.1
pre-commit>=3.7
