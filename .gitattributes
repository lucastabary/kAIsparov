# Shell scripts must keep LF endings so they run on Linux (RunPod pods) regardless
# of the contributor's platform or core.autocrlf setting.
*.sh text eol=lf

# PowerShell helpers run on Windows.
*.ps1 text eol=crlf
